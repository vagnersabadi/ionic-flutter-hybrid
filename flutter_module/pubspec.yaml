name: flutter_module
description: Flutter module embedado no app Ionic 7 (Add-to-App)
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  module:
    androidX: true
    androidPackage: com.example.ionicflutterhybrid.flutter
    iosBundleIdentifier: com.example.ionicflutterhybrid.flutter
